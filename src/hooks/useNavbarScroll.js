/**
 * Stub: navbar scroll behavior. No-op so navbar stays visible.
 * @param {number} [mobileVh] - Ignored.
 * @param {number} [desktopVh] - Ignored.
 */
export function useNavbarScroll(mobileVh, desktopVh) {
    // No-op: navbar visibility unchanged (component keeps its own isVisible state)
}
