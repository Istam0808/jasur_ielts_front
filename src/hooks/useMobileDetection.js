/**
 * Stub: site is PC-only. Always returns false (not mobile).
 * @param {number} [breakpoint] - Ignored; kept for API compatibility.
 * @returns {boolean}
 */
export function useMobileDetection(breakpoint) {
    return false;
}
